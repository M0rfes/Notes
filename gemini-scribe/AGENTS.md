# AGENTS.md

This file provides context about this Obsidian vault for AI agents.

## Vault Overview

This vault is a personal knowledge base and technical workspace focused on Large Language Models (LLMs), AI integration (specifically the Gemini ecosystem), and software engineering principles. It has evolved into a structured environment that combines technical research on system design and microservices with daily journaling and productivity tracking.

## Organization

The vault is organized into functional categories that separate system configuration, technical research, and chronological logs:

- **Daily Notes/**: Contains chronological logs of activities and thoughts, managed via a dedicated template.
- **LLMs/**: A technical repository for notes on artificial intelligence concepts, such as Tokenization.
- **Statements/**: A dedicated folder for formalized records or personal declarations.
- **Templates/**: Stores reusable structures to ensure consistency across the vault (e.g., Daily Note Template).
- **Root Level (// Prefix)**: System-level files, AI interaction logs (like `//GEMINI`), and summaries of technical books or specific study sessions (e.g., `DDIA`, `building-microservices`) often appended with dates in `DD-MM-YYYY` format.

## Key Topics

- Large Language Models (LLMs) and AI Technical Concepts (e.g., Tokenization)
- Gemini AI Integration and Interaction Logs
- Software Architecture and System Design (e.g., Microservices, DDIA)
- Daily Journaling and Productivity Methodologies (e.g., Eat the Frog)
- TypeScript and Programming Language Features (e.g., satisfies operator)
- Obsidian Vault Configuration and Template Management

## User Preferences

The user prefers a highly organized and consistent environment with a strong emphasis on naming conventions and metadata. There is a clear distinction between system-level/reference files (using the `//` prefix) and standard content folders.

The user likely values precision and technical accuracy, particularly regarding AI and software architecture. The use of specific dates in filenames for study notes suggests a preference for tracking learning progress over time. Responses should be detailed, technically grounded, and respect the established organizational structure.

## Custom Instructions

- Pay special attention to the `//GEMINI` file for context regarding AI interactions.
- Maintain the existing naming convention of using the double-slash prefix (`//`) for system files, metadata, or high-level reference summaries.
- Use the `DD-MM-YYYY` date format when suggesting or creating new dated reference notes at the root level to match existing files like `//DDIA-14-03-2026`.
- Always check the `Templates/` folder to ensure any new notes align with existing structures.
- Prioritize technical information found within the `LLMs/` folder and root-level architecture notes when answering specialized questions.
